{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "swag",
      "command": "swag init -g cmd/main.go --parseDependency --parseInternal",
      "type": "shell",
      "cwd": "${workspaceFolder}/src"
    }
  ]
}
